package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary Palette ──
val PrimaryBlue = Color(0xFF0B1A7D)
val OnPrimary = Color(0xFFFFFFFF)
val PrimaryContainerBlue = Color(0xFF283593)
val OnPrimaryContainerBlue = Color(0xFF9AA5FF)
val InversePrimary = Color(0xFFBCC2FF)

// ── Primary Fixed ──
val PrimaryFixed = Color(0xFFDFE0FF)
val PrimaryFixedDim = Color(0xFFBCC2FF)
val OnPrimaryFixed = Color(0xFF000C62)
val OnPrimaryFixedVariant = Color(0xFF303C9A)

// ── Secondary Palette ──
val SecondaryEmerald = Color(0xFF006B5E)
val OnSecondary = Color(0xFFFFFFFF)
val SecondaryContainer = Color(0xFF94F0DF)
val MintTeal = Color(0xFF94F0DF)
val OnSecondaryContainerTeal = Color(0xFF006F62)

// ── Secondary Fixed ──
val SecondaryFixed = Color(0xFF97F3E2)
val SecondaryFixedDim = Color(0xFF7AD7C6)
val OnSecondaryFixed = Color(0xFF00201B)
val OnSecondaryFixedVariant = Color(0xFF005047)

// ── Tertiary Palette ──
val Tertiary = Color(0xFF4C1A00)
val OnTertiary = Color(0xFFFFFFFF)
val TertiaryContainer = Color(0xFF702A00)
val OnTertiaryContainer = Color(0xFFF79261)

// ── Tertiary Fixed ──
val TertiaryFixed = Color(0xFFFFDBCC)
val TertiaryFixedDim = Color(0xFFFFB694)
val OnTertiaryFixed = Color(0xFF351000)
val OnTertiaryFixedVariant = Color(0xFF793105)

// ── Error Palette ──
val ExpiryRed = Color(0xFFBA1A1A)
val OnError = Color(0xFFFFFFFF)
val ExpiryContainerRed = Color(0xFFFFDAD6)
val OnExpiryContainerRed = Color(0xFF93000A)

// ── Surface & Background ──
val SurfaceColor = Color(0xFFFBF8FF)
val SurfaceDim = Color(0xFFDBD9E1)
val SurfaceBright = Color(0xFFFBF8FF)
val SurfaceContainerLowest = Color(0xFFFFFFFF)
val SurfaceContainerLow = Color(0xFFF5F2FB)
val SurfaceContainer = Color(0xFFEFEDF5)
val SurfaceContainerHigh = Color(0xFFE9E7F0)
val SurfaceContainerHighest = Color(0xFFE4E1EA)
val SurfaceVariant = Color(0xFFE4E1EA)
val SurfaceTint = Color(0xFF4955B3)

// ── On Surface & Background ──
val OnSurfaceText = Color(0xFF1B1B21)
val OnSurfaceVariantText = Color(0xFF454652)
val OnBackground = Color(0xFF1B1B21)
val LightSurface = Color(0xFFFBF8FF)

// ── Inverse ──
val InverseSurface = Color(0xFF303036)
val InverseOnSurface = Color(0xFFF2EFF8)

// ── Outline ──
val OutlineColor = Color(0xFF767683)
val OutlineVariantColor = Color(0xFFC6C5D4)

// ── Dark Theme Accent ──
val DarkBackgroundAccent = Color(0xFF13131A)
